[project]
name = "hackerone-cli"
dynamic = ["version"]
description = "An unofficial CLI tool for interacting with the HackerOne API"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.14"
dependencies = [
    "requests",
    "python-dotenv",
]

[project.optional-dependencies]
markdown = ["mdv"]

[project.scripts]
hackerone = "hackerone:main"

[project.urls]
Homepage = "https://github.com/thereisnotime/hackerone-cli"
Source = "https://github.com/thereisnotime/hackerone-cli"
Issues = "https://github.com/thereisnotime/hackerone-cli/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "hackerone.py"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.ruff]
target-version = "py314"
line-length = 120

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "UP",
    "B",
    "SIM",
]
ignore = [
    "E501",
    "SIM105",
    "B028",
    "E722",
    "B904",
]

[tool.ruff.format]
quote-style = "double"

[dependency-groups]
dev = [
    "pre-commit>=4.0",
    "pytest>=8.0",
    "ruff>=0.15.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
